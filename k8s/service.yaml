apiVersion: v1
kind: Service
metadata:
  name: profile-service
spec:
  selector:
    app: profile-service

  ports:
    - name: http
      port: 8080
      targetPort: 8080

    - name: agent
      port: 9911
      targetPort: 9911