apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: profile-ingress

spec:
  rules:
    - host: profile.local

      http:
        paths:
          - path: /
            pathType: Prefix

            backend:
              service:
                name: profile-service
                port:
                  number: 8080