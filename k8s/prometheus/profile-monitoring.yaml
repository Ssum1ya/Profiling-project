apiVersion: v1
kind: Service
metadata:
  name: profile-monitoring
spec:
  clusterIP: None
  selector:
    app: profile-service
  ports:
    - name: actuator
      port: 8080
      targetPort: 8080

    - name: agent
      port: 9911
      targetPort: 9911